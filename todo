* handle saveLines
* fuzz some more
* bracketed paste?
* more tests
* consider cairo
* consider using mtime instead of inotify
* set colors 232 — 255 as gradient from 0 → 15?
* add bg/fg colors separate from 0 and 15?
* do something about selection and scrolling
* add a dirtyness system to only redraw when needed
* only clear the selection when *drawing* something inside it
